- name: disabiling nodejs 18 version
  ansible.builtin.command: dnf module disable nodejs:18 -y

- name: enabiling nodejs 20 version
  ansible.builtin.command: dnf module enable nodejs:20 -y

- name: installing nodejs
  ansible.builtin.dnf:
    name: "{{item}}"
    state: installed
  loop:
  - nodejs
  - mysql

- name: create expense user
  ansible.builtin.user:
    name: expense

- name: create app directory
  ansible.builtin.file:
    path: /app
    state: directory

- name: downloading backend.zip file
  ansible.builtin.get_url:
    url: https://expense-builds.s3.us-east-1.amazonaws.com/expense-backend-v2.zip
    dest: /tmp/backend.zip

- name: unzipping backend.zip file
  ansible.builtin.unarchive:
    src: /tmp/backend.zip
    dest: /app
    remote_src: yes

- name: installing npm dependencies
  ansible.builtin.command: npm install
  args:
    chdir: /app

- name: copy backend.service file to remote systemd
  ansible.builtin.template:
    src: backend.service.j2
    dest: /etc/systemd/system/backend.service

- name: daemon reloading
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: start and enable backend service
  ansible.builtin.service:
    name: backend
    state: started
    enabled: yes

- name: installing python mysql dependencies
  ansible.builtin.pip:
    name:
    - PyMySQL
    - 
    executable: pip3.9

- name: loading schema
  community.mysql.mysql_db:
    login_host: "{{login_host}}"
    login_user: root
    login_password: "{{mysql_root_password}}"
    state: import
    target: /app/schema/backend.sql
    name: all

- name: restarting backend
  ansible.builtin.service:
    name: backend
    state: restarted